# =============================================================================
# Configuration Hyperliquid Trading Bot
# =============================================================================
# Copiez ce fichier en .env et remplissez avec vos vraies valeurs
# Commande: cp .env.example .env

# =============================================================================
# SÉCURITÉ - Configuration d'authentification et CORS
# =============================================================================

# Clé API pour authentifier les requêtes de trading
# IMPORTANT: Générez une clé aléatoire forte (minimum 32 caractères)
# Exemple de génération: openssl rand -hex 32
API_KEY=your_secret_api_key_here_minimum_32_characters_required

# Origines autorisées pour CORS (séparées par des virgules)
# En développement: localhost uniquement
# En production: ajoutez vos domaines
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,http://127.0.0.1:3000

# Activer/désactiver le trading
# true = trading activé (SECRET_KEY requis)
# false = mode read-only uniquement (SECRET_KEY optionnel)
TRADING_ENABLED=true

# =============================================================================
# HYPERLIQUID - Configuration de trading (OBLIGATOIRE si TRADING_ENABLED=true)
# =============================================================================

# Adresse Ethereum de votre compte Hyperliquid (format: 0x...)
ACCOUNT_ADDRESS=0xYourEthereumAddressHere

# Clé privée de votre wallet
# ATTENTION: Ne partagez JAMAIS cette clé, ne la committez JAMAIS sur Git
# Requis uniquement si TRADING_ENABLED=true
SECRET_KEY=0xYourPrivateKeyHere

# =============================================================================
# SURVEILLANCE - Adresses à écouter pour les notifications
# =============================================================================

# Liste des adresses Ethereum à surveiller (format JSON array)
# Example: ["0xAddress1", "0xAddress2", "0xAddress3"]
USERS_LISTENED=["0xAddress1ToMonitor", "0xAddress2ToMonitor"]

# =============================================================================
# TELEGRAM - Configuration pour les notifications (OPTIONNEL)
# =============================================================================

# Token du bot Telegram (obtenu via @BotFather)
# Example: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# ID du chat Telegram où envoyer les notifications
# Trouvez votre chat ID en parlant au bot @userinfobot
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# =============================================================================
# API - Configuration du serveur FastAPI
# =============================================================================

# Host sur lequel l'API écoute
# 0.0.0.0 = accessible depuis l'extérieur (production)
# 127.0.0.1 = localhost uniquement (développement)
API_HOST=0.0.0.0

# Port sur lequel l'API écoute
API_PORT=8000

# =============================================================================
# NOTES IMPORTANTES
# =============================================================================
# - API_KEY est OBLIGATOIRE pour la sécurité (minimum 32 caractères)
# - SECRET_KEY est requis UNIQUEMENT si TRADING_ENABLED=true
# - En mode read-only (TRADING_ENABLED=false), SECRET_KEY peut être omis
# - ACCOUNT_ADDRESS doit correspondre à la clé privée
# - TELEGRAM_* sont optionnels, les notifications ne seront pas envoyées si absents
# - USERS_LISTENED doit être un JSON array valide
# - ALLOWED_ORIGINS doit contenir les domaines autorisés (jamais "*" en production)
